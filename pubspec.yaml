name: movie_poster_studio
description: "Movie Poster Studio — Flutter port with retrowave UI."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  image: ^4.2.0
  file_picker: ^8.1.2
  path: ^1.9.0
  path_provider: ^2.1.4
  window_manager: ^0.4.2
  ffi: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
